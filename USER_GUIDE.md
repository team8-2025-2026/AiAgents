# Пошаговая инструкция: Регистрация и вход пользователя

## Шаг 1: Создание первого пользователя (Ассистента)

### Вариант A: Через веб-интерфейс API (Swagger UI)

1. **Откройте документацию API:**
   - Перейдите в браузере на: **http://localhost:8000/docs**
   - Вы увидите интерактивную документацию Swagger UI

2. **Найдите эндпоинт для создания пользователя:**
   - Найдите раздел `PUT /user` (зеленая кнопка)
   - Нажмите на него, чтобы развернуть

3. **Нажмите кнопку "Try it out"**

4. **Заполните параметры:**
   - `email`: `admin@example.com` (или любой другой email)
   - `first_name`: `Иван`
   - `last_name`: `Иванов`
   - `status`: `ASSISTENT` (выберите из выпадающего списка)
   - `access_token`: `admin_secret_token_12345` (значение из файла `.env`)

5. **Нажмите кнопку "Execute"**

6. **Скопируйте пароль из ответа:**
   - В ответе будет поле `password` - это сгенерированный пароль
   - **ВАЖНО:** Сохраните этот пароль! Он больше не будет показан
   - Пример ответа:
   ```json
   {
     "success": true,
     "data": {
       "id": 1,
       "email": "admin@example.com",
       "first_name": "Иван",
       "last_name": "Иванов",
       "status": "ASSISTENT",
       "password": "aB3xK9mP2qR7vN5tY8wZ1",
       "access_token": "..."
     }
   }
   ```

### Вариант B: Через curl (командная строка)

Откройте PowerShell и выполните:

```powershell
$body = @{
    email = "admin@example.com"
    first_name = "Иван"
    last_name = "Иванов"
    status = "ASSISTENT"
    access_token = "admin_secret_token_12345"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8000/user?email=admin@example.com&first_name=Иван&last_name=Иванов&status=ASSISTENT&access_token=admin_secret_token_12345" -Method PUT
```

Или используйте curl (если установлен):

```bash
curl -X PUT "http://localhost:8000/user?email=admin@example.com&first_name=Иван&last_name=Иванов&status=ASSISTENT&access_token=admin_secret_token_12345"
```

---

## Шаг 2: Вход в систему

### Способ 1: Через веб-интерфейс (http://localhost:8000)

1. **Откройте браузер и перейдите на:**
   - **http://localhost:8000**

2. **Вы увидите форму входа:**
   - Поле "Email"
   - Поле "Пароль"
   - Кнопка "Войти"

3. **Введите данные:**
   - **Email:** `admin@example.com` (тот, который вы использовали при создании)
   - **Пароль:** пароль, который был сгенерирован при создании пользователя

4. **Нажмите кнопку "Войти"**

5. **После успешного входа:**
   - Вы будете перенаправлены на http://localhost:3000 (если WebUI запущен)
   - Или увидите сообщение об успешном входе

### Способ 2: Через API (для тестирования)

1. **Откройте документацию API:**
   - Перейдите на: **http://localhost:8000/docs**

2. **Найдите эндпоинт `GET /user`**

3. **Нажмите "Try it out"**

4. **Заполните параметры:**
   - `email`: `admin@example.com`
   - `password`: ваш пароль

5. **Нажмите "Execute"**

6. **В ответе вы получите:**
   - `access_token` - токен для дальнейших запросов
   - Данные пользователя

---

## Шаг 3: Создание дополнительных пользователей (Студент/Учитель)

После того, как вы создали ассистента и вошли, вы можете создавать других пользователей.

### Через веб-интерфейс (если WebUI запущен)

1. **Войдите в систему** как ассистент

2. **Перейдите на страницу управления:**
   - Откройте: **http://localhost:3000/manage**

3. **Заполните форму создания пользователя:**
   - Email
   - Имя
   - Фамилия
   - Статус (STUDENT/TEACHER/ASSISTENT)

4. **Нажмите "Создать пользователя"**

5. **Скопируйте сгенерированный пароль** и отправьте его пользователю

### Через API (Swagger UI)

1. **Откройте:** **http://localhost:8000/docs**

2. **Найдите `PUT /user`**

3. **Нажмите "Try it out"**

4. **Заполните параметры:**
   - `email`: `student@example.com`
   - `first_name`: `Петр`
   - `last_name`: `Петров`
   - `status`: `STUDENT` (или `TEACHER`)
   - `access_token`: токен ассистента (из шага 2, поле `access_token`)

5. **Нажмите "Execute"**

6. **Скопируйте пароль** из ответа

---

## Шаг 4: Вход созданного пользователя

1. **Откройте:** **http://localhost:8000**

2. **Введите:**
   - Email созданного пользователя
   - Пароль, который был сгенерирован

3. **Нажмите "Войти"**

---

## Примеры использования

### Пример 1: Создание студента

**Через Swagger UI:**
```
PUT /user
email: student1@example.com
first_name: Мария
last_name: Сидорова
status: STUDENT
access_token: <токен_ассистента>
```

**Результат:**
- Пользователь создан
- Пароль: `Xk9mP2qR7vN5tY8wZ1aB3` (пример)
- Отправьте этот пароль пользователю

### Пример 2: Создание учителя

**Через Swagger UI:**
```
PUT /user
email: teacher1@example.com
first_name: Анна
last_name: Козлова
status: TEACHER
access_token: <токен_ассистента>
```

---

## Важные замечания

1. **Первый пользователь** должен быть создан с `access_token = admin_secret_token_12345` (из `.env`)

2. **Последующие пользователи** создаются с `access_token` ассистента

3. **Пароли генерируются автоматически** и показываются только один раз при создании

4. **Регистрация через веб-интерфейс недоступна** - только через поддержку (ассистентов)

5. **Для полного функционала** (чаты, профиль) запустите WebUI на порту 3000

---

## Быстрый старт (краткая версия)

1. Откройте **http://localhost:8000/docs**
2. Найдите `PUT /user`, нажмите "Try it out"
3. Заполните:
   - email: `admin@example.com`
   - first_name: `Admin`
   - last_name: `User`
   - status: `ASSISTENT`
   - access_token: `admin_secret_token_12345`
4. Нажмите "Execute"
5. Скопируйте `password` из ответа
6. Откройте **http://localhost:8000**
7. Введите email и пароль
8. Нажмите "Войти"

Готово! 🎉



